# Структура для Vercel развёртывания

## Инструкция

### 1. Подготовка репозитория на GitHub

Структура файлов:
```
sampling-app/
├─ vercel.json                  (конфигурация Vercel)
├─ package.json                 (зависимости)
├─ .gitignore                   (игнорируемые файлы)
├─ frontend/
│  ├─ index.html               (PWA приложение)
│  ├─ manifest.json            (PWA манифест)
│  └─ service-worker.js        (офлайн поддержка)
├─ api/
│  ├─ db.js                    (работа с БД)
│  ├─ scans-bulk.js            (POST /api/scans/bulk)
│  ├─ scans-count.js           (GET /api/scans/count)
│  └─ health.js                (GET /api/health)
└─ README.md                   (документация)
```

### 2. Шаги развёртывания

#### Шаг 1: Клонировать репозиторий
```bash
git clone https://github.com/ваш-ник/sampling-app.git
cd sampling-app
```

#### Шаг 2: Создать package.json
Используйте содержимое из `package-vercel.json`, переименуйте в `package.json`.

#### Шаг 3: Сохранить API файлы
Создайте папку `api/` и положите туда файлы:
- `db.js` (из `api-db.js`)
- `scans-bulk.js` (из `api-scans-bulk.js`)
- `scans-count.js` (из `api-scans-count.js`)
- `health.js` (из `api-health.js`)

#### Шаг 4: Сохранить фронтенд
Создайте папку `frontend/` и положите туда:
- `index.html` (из предыдущей версии с правкой API_BASE_URL)
- `manifest.json`
- `service-worker.js`

#### Шаг 5: Сохранить конфигурацию Vercel
Положите файл `vercel.json` в корень.

#### Шаг 6: Пушить в GitHub
```bash
git add .
git commit -m "Initial commit for Vercel deployment"
git push -u origin main
```

### 3. Развёртывание на Vercel

1. Зайдите на https://vercel.com
2. Нажмите "Add New Project"
3. Выберите ваш репозиторий из GitHub
4. В Project Settings:
   - **Framework Preset**: Other
   - **Root Directory**: ./ (корень репо)
5. Нажмите "Deploy"

### 4. Добавление переменных окружения

После развёртывания в **Settings → Environment Variables** добавьте:

```
API_KEY = test-key
DATABASE_URL = postgresql://user:password@host:5432/dbname
NODE_ENV = production
```

Где `DATABASE_URL` — строка подключения к PostgreSQL (напр. Supabase, Railway, Render, etc.)

### 5. Правка фронтенда под Vercel

В `frontend/index.html` найдите строку:
```javascript
const API_BASE_URL = 'http://localhost:3000';
```

Измените на:
```javascript
const API_BASE_URL = ''; // Пусто = используются относительные пути /api/
```

### 6. Инициализация БД

После первого развёртывания API автоматически создаст таблицы при первом запросе.

Проверьте:
```
https://your-project.vercel.app/api/health
```

Должно вернуть:
```json
{
  "status": "ok",
  "message": "Sampling API is running",
  "version": "1.0.0"
}
```

### 7. Проверка синхронизации

Откройте фронтенд:
```
https://your-project.vercel.app
```

Тестируйте:
- Запишите пробу
- Проверьте, что индикатор синхронизации показывает "pending"
- Синхронизация должна произойти автоматически
- Проверьте DevTools → Network, должны быть запросы к `/api/scans/bulk`

### 8. Альтернативные БД сервисы

**Supabase** (PostgreSQL в облаке):
```
Зарегистрируйтесь на supabase.com
Создайте новый проект
DATABASE_URL скопируйте из Settings → Database
```

**Railway**:
```
Зарегистрируйтесь на railway.app
Добавьте PostgreSQL сервис
DATABASE_URL из Settings
```

**Render**:
```
Зарегистрируйтесь на render.com
Создайте PostgreSQL Database
Скопируйте Internal Connection String
```

### 9. Переменные в .env для локальной разработки

Если разрабатываете локально, создайте `.env` файл:
```
NODE_ENV=development
API_KEY=test-key
DATABASE_URL=postgresql://user:password@localhost:5432/sampling_db
```

### 10. Массовый импорт данных

После развёртывания можно заполнить БД данными:

```bash
curl -X POST https://your-project.vercel.app/api/scans/bulk \
  -H "Content-Type: application/json" \
  -H "X-API-Key: test-key" \
  -d '[
    {
      "device_id": "device_1",
      "sample": "SAMPLE001",
      "well_name": "WL001",
      "block": "BLK01",
      "type": "520",
      "scanned_at": 1703780000000,
      "scanned_by": "John",
      "is_test": false,
      "local_id": 1
    }
  ]'
```

### 11. Логирование и мониторинг

В Vercel Dashboard → Logs смотрите:
- Функции API
- Ошибки развёртывания
- Производительность

### Готово!

Приложение развёрнуто и готово к использованию:
- 🌐 **Фронтенд**: https://your-project.vercel.app
- 🔌 **API**: https://your-project.vercel.app/api/
- 💾 **БД**: PostgreSQL в облаке (Supabase, Railway, Render, etc.)
- ⚡ **Масштабирование**: Автоматическое

Версия: 1.0.0
Дата: Декабрь 2024
